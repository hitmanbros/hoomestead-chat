// Could show loading indicator while fetching rooms
{rooms.length === 0 && (
    <div style={{ padding: "8px 16px", color: "var(--text-muted)", fontSize: "14px" }}>
        No channels yet
    </div>
)}
